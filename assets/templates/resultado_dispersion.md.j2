### Tabla de dispersión ({{ "Población" if poblacional else "Muestra" }}){% if intervalos %} — agrupada por intervalos{% endif %}

| {% if intervalos %}Intervalo{% else %}xi{% endif %} | f | Xi - x̄ | (Xi - x̄)² | fi·(Xi - x̄)² |
|---:|---:|---:|---:|---:|
{% for row in rows -%}
| {{ row.label }} | {{ row.f }} | {{ row.diff }} | {{ row.diffSq }} | {{ row.fDiffSq }} |
{% endfor %}
**n:** {{ n }}
**Media (x̄):** {{ mean }}
**Rango:** {{ rango }}
**Varianza ({{ "σ²" if poblacional else "s²" }}):** {{ varianza }}
**Desvío ({{ "σ" if poblacional else "s" }}):** {{ desvio }}
**CV (%):** {{ cv }}
